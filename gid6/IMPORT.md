# Происхождение gid6

Основные исходники импортированы из локального рабочего дерева
`H:\projects\tgid-app-new\gid6`.

- исходный commit: `18f7a6ceb76309a288fce480a0659be53bbf6d96`;
- дата commit: `2026-03-18T10:39:24+05:00`;
- исходный remote `git:gid6.git` недоступен;
- вложенный `.git` и сборочные файлы не импортированы.

В репозиторий включён основной исходный каталог `gid6/gid6`. Архивные
конвертеры, городские конфигурации, RDP/OAuth-файлы и прочие вспомогательные
каталоги не импортировались: в них обнаружены буквальные учётные данные.

Восемь прямых значений паролей в импортируемых исходниках `gid6`/`gid8`
заменены пустыми значениями. Пароль должен поступать из диалога подключения
или окружения, а не из Git.
