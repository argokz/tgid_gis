# Происхождение gid8

Основные исходники импортированы из локального рабочего дерева
`H:\projects\tgid-app-new\gid8`.

- исходный commit: `a9f199b05002cabf8f885cb8b173ddf983d40ef1`;
- дата commit: `2026-06-24T09:31:44+05:00`;
- исходный remote `git:gid8.git` недоступен;
- вложенный `.git` и сборочные файлы не импортированы.

Включены продуктовый Qt-клиент `gid8/gid8` и расчётное ядро
`gid8/python/sety`. Импортировано текущее рабочее состояние, включая
незакоммиченные правки чтения фрагментов, запись в `net`/`calc` и
сгенерированный `python/sety/sety/net_mode.py`.

Старый интерфейс, состав окон, ribbon, карта, паспорта, меню и команды
являются продуктовым контрактом и должны сохраняться. Архивные конвертеры,
городские конфигурации, OAuth/GeoServer-файлы и другие каталоги с локальными
учётными данными оставлены только в исходном локальном репозитории.
