SELECT 

MON,
round(VenP,1),
round(VdrP,1),
round(VpP,1),
round(Qen,1),
round(Qdr,1),
round(Expr1006,1) AS e1,
round(Qp,1),
round(Expr1008,1) AS e2

FROM [POT_Нормируемые месячные тепловые потери с утечкой сетевой воды]
