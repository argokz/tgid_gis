SELECT 

key_ut_p,
kod1,
uzel1,
pr1,
kod2,
uzel2,
pr2,
typ_param,
year,
dateend_izol,
diam_vn,
ROUND(dlina_p,3),
ROUND(MP,1),
ROUND(MO,2),
ROUND(M,2),
ROUND(MMM,2),
ROUND(V,1),
ROUND(Vlet,2)

FROM [POT_Материальная характеристика]

